const base = '/setting';

const ROUTES = {
  adminPanel: base + '/admin-panel',
  setting: base + '/setting',

  add: base + '/new',
  edit: base + '/edit/:id',
  editLink: base + '/edit/'
};

export default ROUTES;
