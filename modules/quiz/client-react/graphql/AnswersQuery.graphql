query answers($quizId: Int!, $userId: Int!) {
  answers(quizId: $quizId, userId:$userId) {
    answers {
      id
      userId
      choiceId
      questionId
      content
    }
    # active
    # title
    # description
    # questions{
    #   id
    #   description
    #   choices{
    #     id
    #     description
    #   }
    # }
  }
}
