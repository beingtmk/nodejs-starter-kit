query quizzes($filter: FilterQuizInput) {
  quizzes(filter: $filter) {
    id
    userId
    active
    title
    description
    cover
    user {
      id
      username
    }
    sections {
      id
      questions {
        id
      }
    }
  }
}
