const base = '/review';

const ROUTES = {
  adminPanel: base + '/admin-panel',

  review: base + '/review',
  myReview: base + '/my-review',

  add: base + '/new',
  edit: base + '/edit/:id',
  editLink: base + '/edit'
};

export default ROUTES;
