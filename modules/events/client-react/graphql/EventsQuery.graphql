# GraphQL queries
query events {
  events {
    id
    userId
    title
    details
    venue
    date
    time
    admins {
      id
      userId
      eventId
      username
      contactInfo
    }
    participants {
      id
      eventId
      userId
    }
  }
}
