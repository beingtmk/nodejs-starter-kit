const base = '/category';

const ROUTES = {
  adminPanel: base + '/admin-panel',
  category: base + '/category',

  add: base + '/new',
  edit: base + '/edit/:cid',
  editLink: base + '/edit/'
};

export default ROUTES;
