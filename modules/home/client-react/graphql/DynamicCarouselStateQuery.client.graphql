#import "./DynamicCarousel.client.graphql"

query DynamicCarouselState {
  dynamicCarouselState @client {
    orderBy {
      column
      order
    }
    filter {
      searchText
      label
      isActive
    }
    # ...DynamicCarouselStateInfo
  }
}
