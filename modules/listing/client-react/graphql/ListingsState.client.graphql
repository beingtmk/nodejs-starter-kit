fragment ListingsStateInfo on ListingsState {
  orderBy {
    column
    order
  }
  filter {
    # userId
    searchText

    categoryFilter {
      categoryId
      allSubCategory
    }

    lowerCost
    upperCost

    # isFeatured
    # isNew
    # isDiscount
    isActive
  }
}
