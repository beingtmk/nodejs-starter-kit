fragment ListingsStateInfo on ListingsState {
  orderBy {
    column
    order
  }
  filter {
    # userId
    searchText

    discount

    categoryFilter {
      categoryId
      allSubCategory
    }

    lowerCost
    upperCost

    # isFeatured
    # isNew
    # isDiscount
    isActive
  }
}
