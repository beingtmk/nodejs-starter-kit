fragment ListingsStateInfo on ListingsState {
  orderBy {
    column
    order
  }
  filter {
    # userId
    searchText
    categoryId

    lowerCost
    upperCost

    # isFeatured
    # isNew
    # isDiscount
    isActive
  }
}
